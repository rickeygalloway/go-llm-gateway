title = "Gitleaks Config"

[extend]
useDefault = true

[allowlist]
description = "Safe files that may contain example secrets"
paths = [
    "config.yaml",       # config.yaml contains only placeholder/commented API keys
    "deploy/helm/values.yaml",
]
